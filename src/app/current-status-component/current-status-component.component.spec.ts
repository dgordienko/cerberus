/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CurrentStatusComponentComponent } from './current-status-component.component';

describe('Component: CurrentStatusComponent', () => {
  it('should create an instance', () => {
    let component = new CurrentStatusComponentComponent();
    expect(component).toBeTruthy();
  });
});
